# TrendCrafter AI 素材库 · 产品逻辑与工程实现文档 (v1.0)

## 1. 产品核心定位
本系统是一个基于**多模态大模型 (Multimodal LLM)** 与 **向量检索 (RAG)** 的智能营销素材管理平台。
核心价值在于解决素材管理的“存、找、用”难题，并通过AI预测能力实现“优胜劣汰”的库存良性循环。

---

## 2. 全链路素材入库 (Ingestion Pipeline)
素材入库不再是简单的文件上传，而是经过深度结构化处理的“数字化”过程。

### 2.1 处理流程
1.  **视觉理解 (Vision Understanding)**
    *   **模型**: Qwen-VL-Max / GPT-4V
    *   **任务**: 生成详细的视觉描述 (Caption)，包括场景、人物、动作、氛围。
2.  **OCR 文字提取**
    *   **工具**: OCR.space / DashScope OCR
    *   **任务**: 提取海报中的所有可见文字，用于后续文案分析。
3.  **智能打标 (Auto-Tagging)**
    *   **模型**: Qwen-Turbo
    *   **逻辑**: 基于“阶梯式Prompt”，从 OCR 和 视觉描述中提取结构化标签：
        *   `visual_style` (视觉风格): 极简、赛博朋克、国潮等。
        *   `color_tone` (色调): 莫兰迪、高饱和、暖色调等。
        *   `composition` (构图): 中心构图、三分法等。
        *   `material_purpose` (用途): 活动Banner、主图等。
        *   `target_audience` (受众): Z世代、宝妈等。
4.  **多Agent 效果预测 (Multi-Agent Scoring)**
    *   **核心创新**: 摒弃单一黑盒预测，采用“专家评审团”模式。
    *   **总分**: 30分
    *   **角色定义**:
        *   **👀 视觉设计师 (Visual Designer)**: 10分
            *   *关注点*: Stopping Power (停下来)。
            *   *标准*: 色彩冲击力、主体突出度。
        *   **✍️ 文案策略师 (Copy Strategist)**: 10分
            *   *关注点*: Readability (读进去)。
            *   *标准*: 利益点直白度、痛点精准度、信息层级。
        *   **💰 投放优化师 (Ad Optimizer)**: 10分
            *   *关注点*: Click Desire (点下去)。
            *   *标准*: CTA按钮明显度、紧迫感、商业规范性。
    *   **输出**: 综合评分 (0-30) + 详细评审理由。
5.  **向量化 (Embedding)**
    *   **模型**: Text-Embedding-v1
    *   **输入**: `视觉标签 + OCR文案 + 核心卖点`
    *   **存储**: Qdrant 向量数据库 (Payload包含所有元数据及分数)。

---

## 3. 智能供给决策 (Supply Decision Matrix)
当用户提出需求时，系统不再盲目生成，而是基于**库存现状**做最优决策。

### 3.1 二维决策矩阵
系统评估两个维度：
*   **X轴: 语义相似度 (Similarity)** - 库存里有没有像的？
*   **Y轴: 预测分数 (Predicted Score)** - 像的那些质量好不好？

### 3.2 决策逻辑 (Thresholds)
设定阈值: 高分=24 (80%), 中分=18 (60%), 高相似=0.7, 中相似=0.5

| 场景 | 相似度 (Sim) | 预测分 (Score) | 决策动作 (Action) | 策略详解 |
| :--- | :--- | :--- | :--- | :--- |
| **完美库存** | High (>0.7) | High (>24) | **✅ 直接复用 (REUSE)** | 库存中有高质量且匹配的素材，无需浪费算力生成。 |
| **有形无神** | High (>0.7) | Low (<18) | **⚠️ 避坑生成 (GENERATE)** | 库存虽有相似素材但效果差，需吸取其教训（Negative Prompt）重新设计。 |
| **潜力股** | Mid (0.5-0.7) | High (>18) | **🎨 智能微调 (FISSION)** | 素材不错但需微调（如改文案、换背景），通过 Fission Agent 快速裂变。 |
| **库存空白** | Low (<0.5) | Any | **🧬 RAG参考生成 (GENERATE)** | 无相关库存，启动 RAG 流程，检索高分“基因”进行全新生成。 |

---

## 4. 核心功能模块

### 4.1 智能搜索 (Search Agent)
采用 L1/L2/L3 动态路由策略：
*   **L1 (属性过滤)**: 用户搜“红色海报” -> 直接过滤 `color_tone="红色"` (100%准确)。
*   **L2 (语义检索)**: 用户搜“夏天清爽的感觉” -> 向量检索 (理解抽象概念)。
*   **L3 (复杂推理)**: 用户搜“适合30岁女性的低脂零食广告” -> LLM 先拆解需求 -> 再检索。

### 4.2 FISSION (智能微调)
*   **输入**: 参考素材ID + 修改指令 (如“背景换成星空”)。
*   **流程**:
    1.  提取参考素材的 `Prompt` 和 `Tags`。
    2.  LLM 理解修改指令，重写 `New Prompt`。
    3.  调用绘图模型生成变体。

### 4.3 RAG 参考生成
*   **输入**: 设计需求。
*   **流程**:
    1.  检索库存中历史表现最好 (Score > 24) 的素材。
    2.  提取其“高分基因” (风格、色调、构图)。
    3.  将“高分基因”与“用户需求”融合，生成优化后的 Prompt。
    4.  生成新素材，确保新素材大概率具备高转化特征。

---

## 5. 异常处理与系统健壮性 (Robustness)

### 5.1 API 熔断与降级
*   **问题**: 大模型 API (如 Qwen-VL) 偶尔超时或报错。
*   **策略**:
    *   **重试机制**: 关键路径 (如打分) 自动重试 3 次。
    *   **降级方案**: 如果 VL 模型失败，降级为仅使用 OCR + 文本标签进行基础入库，标记为 `pending_score`，后续任务队列补跑。

### 5.2 数据一致性
*   **问题**: 向量库与元数据不一致。
*   **策略**: 使用 Qdrant Payload 存储全量元数据，以向量库为 Single Source of Truth，避免数据库同步问题。

---

## 6. 数据闭环 (Feedback Loop)
系统具备自我进化能力，而非静态工具。

1.  **用户反馈**:
    *   用户对生成结果的“采用/拒绝”行为被记录。
    *   采用 = 正样本，拒绝 = 负样本。
2.  **权重校准**:
    *   如果某高分素材在实际业务中点击率低，系统定期对比 `Predicted Score` vs `Real CTR`。
    *   自动微调多 Agent 的 Prompt 权重 (例如：发现“色彩冲击力”权重过高，自动下调)。

---

## 7. 技术栈
*   **Frontend**: Streamlit
*   **LLM/VLM**: Qwen-Turbo, Qwen-VL-Max
*   **Embedding**: DashScope Text-Embedding
*   **Vector DB**: Qdrant
*   **Image Gen**: Wanx (通义万相) / Stable Diffusion (可扩展)
